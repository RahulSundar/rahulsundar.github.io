title: Rahul Sundar
email: rahul@example.com
description: >- # this means to ignore newlines until "baseurl:"
  Personal website and portfolio of Rahul Sundar, Researcher.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://rahulsundar.in" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: 
github_username:  RahulSundar

# Build settings
markdown: kramdown
theme: null # We are using a custom layout, so no pre-built theme.

# Exclude from processing.
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

permalink: pretty
